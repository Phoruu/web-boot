    // STRING TRUNCATE-R
// var string1 = prompt("Enter a string : ");
// alert("You have " + string1.length +" characters, there remains " + (140-string1.length) + " characters remaining");
// alert("The string limit is only 5! Your text is " + string1.slice(0,8));

    // Capital-izer
// yourName = prompt("Please enter your name!");
// alert("Hello, " + yourName.slice(0,1).toUpperCase() + yourName.slice(1, yourName.length).toLowerCase());

